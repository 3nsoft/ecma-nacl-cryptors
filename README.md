# JS and WASM NaCl cryptors

JavaScript and WASM implementations of cryptor from ecma-nacl to be used in things like [core-3nweb-client-lib](https://github.com/3nsoft/core-3nweb-client-lib/).
This library gives options of encryptors in this process, and parallel via Node's `worker_threads`.
